<main class="lista-vacantas">
  <h2>Lista de vacantes</h2>
  {{#if vacantes.length }}
   {{#each vacantes}}
      <div class="vacante">
          <div class="caja">
            <h3>{{empresa}}</h3>
            <p class="puesto">{{titulo}}</p>
          </div>
          <div class="caja">
            <p class="etiqueta">Ubicacion</p>
            <p class="nombre">{{ubicacion}}</p>
          </div>
          <div class="caja">
            <p class="etiqueta">Contrato</p>
            <p class="nombre contrrato">{{contrato}}</p>
          </div>
          <div class="caja">
            <p class="centrar-vertical">
              <a href="/vacantes/{{url}}" class="btn btn-verde">Más info...</a>
            </p>
          </div>
      </div> {{!-- Vacante --}}
    {{/each}}
  {{else}}
    <p>No hay vacantes en este momento</p>
  {{/if}}
</main>