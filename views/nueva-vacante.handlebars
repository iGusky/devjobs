<main class="contenedor">
  <form action="/vacantes/nueva" method="POST" class="default-form">
    <h3>Información General</h3>
    
    <div class="campo">
      <label>Título</label>
      <input type="text" name="titulo" placeholder="Ej. Angular Developer" required>
    </div>

    <div class="campo">
      <label>Empresa</label>
      <input type="text" name="empresa" placeholder="Ej. Google">
    </div>

    <div class="campo">
      <label>Ubicación</label>
      <input type="text" name="ubicacion" placeholder="Ej. México o remoto" required>  
    </div>

    <div class="campo">
      <label>Salario (USD)</label>
      <input type="text" name="salario" placeholder="Ej. 1000" required>
    </div>

    <div class="campo">
      <label>Contrato</label>
      <select name="contrato" id="">
        <option value="" disabled selected>--Selecciona--</option>
        <option value="Freelance">Freelance</option>
        <option value="Tiempo completo">Tiempo completo</option>
        <option value="Medio tiempo">Medio tiempo</option>
        <option value="Por proyecto">Por proyecto</option>
      </select>
    </div>

    <h3>Descripción del puesto</h3>

    <div class="campo descripcion">
      <label for="">Descripción del puesto</label>
      <input type="hidden" id="x" name="descripcion">
      <trix-editor input="x"></trix-editor>
    </div>

    <h3>Conocimientos</h3>
    <ul class="lista-conocimientos">
      {{#seleccionarSkills a}}
      {{/seleccionarSkills}}
    </ul>

    <div class="campo centrar-horizontal">
      <input type="hidden" name="skills" id="skills">
      <input type="submit" value="Publicar" class="btn btn-azul">
    </div>

  </form>
</main>